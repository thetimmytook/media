# Repository workflow

- Work only on feature branches. Never push changes directly to `main`.
- Name feature branches without the `codex/` prefix.
- Deliver all changes through pull requests targeting `main`.
- Keep requested changes uncommitted while the user reviews and tests them locally.
- Create commits and push updates only after the user gives explicit confirmation.
